---
spring:
  profiles:
    active: india
  application:
    name: validation-service
  cloud:
    config:
      uri: http://localhost:8001
 
     

server:
  port: 8002
  
logging:
  config: classpath:logback.xml
  file: validationService.log
  
